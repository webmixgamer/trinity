name: Test Worker Agent
display_name: Test Worker
description: Worker agent for testing the Workplan system (Pillar I)
version: "1.0.0"
author: Trinity Platform

resources:
  cpu: "1"
  memory: "2g"

mcp_servers: []

credentials: {}

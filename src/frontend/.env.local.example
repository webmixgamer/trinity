# Frontend Environment Configuration
# Copy this file to .env.local for local customizations

# NOTE: Authentication mode is now controlled by the BACKEND, not frontend
# To enable dev mode (username/password login):
#   Set DEV_MODE_ENABLED=true in docker-compose.yml backend environment
#
# The frontend automatically detects the mode via GET /api/auth/mode
# No frontend rebuild is needed to switch between dev and prod modes

# ===========================================
# AUTH0 CONFIGURATION (for production)
# ===========================================
# Required if using Auth0 authentication
VITE_AUTH0_DOMAIN=your-tenant.us.auth0.com
VITE_AUTH0_CLIENT_ID=your-client-id
VITE_AUTH0_ALLOWED_DOMAIN=your-company.com  # Leave empty to allow all domains

# ===========================================
# API URL (optional - defaults to same origin)
# ===========================================
# VITE_API_BASE_URL=http://localhost:8000
